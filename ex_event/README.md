## JQuery event handling sample

+ Target events
    + Window events (load, resize, etc)
    + Keyborad events (keydown, keyup, etc)
    + Mouse events (mousedown, mouseup, mousemove, wheel, etc)

### how to run

```
> npm install
> run
```
